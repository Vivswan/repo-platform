# The fixture's override: main requires all-green, pinned to the Actions app.
repository:
  allow_merge_commit: false
rulesets:
  _undeclared: delete
  entries:
    - name: main
      target: branch
      enforcement: active
      rules:
        - type: deletion
        - type: required_status_checks
          parameters:
            strict_required_status_checks_policy: false
            required_status_checks:
              - context: all-green
                integration_id: 15368
