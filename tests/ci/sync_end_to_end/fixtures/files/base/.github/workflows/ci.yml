name: "{{project_name}} CI"
on:
  push:
    branches: [main]
permissions: {}
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.sha }} for {{github_username_lower}}/{{project_slug}}"
      - run: echo "tracking {{fuzzer_label}}"
