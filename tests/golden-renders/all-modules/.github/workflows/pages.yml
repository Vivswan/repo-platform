# This file is managed by Vivswan/repo-platform.
# Local edits may be replaced during template updates.
#
# The versioned Pages layout, the build contract (PAGES_BASE_PATH,
# PAGES_ORIGIN, PAGES_VERSION), and the CUSTOM_DOMAIN cutover:
# repo-platform's docs/pages.md. One-time setup without the settings-sync
# module: repo Settings -> Pages -> Source: GitHub Actions (with it, the
# module's settings layer enables Pages automatically).
name: Pages

on:
  push:
    branches: [main]
  schedule:
    # Nightly rebuild: version tags created without a push land, and
    # central pipeline updates propagate (docs/pages.md).
    - cron: "23 4 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    uses: Vivswan/repo-platform/.github/workflows/reusable-pages.yml@main
    with:
      mounts: '[{"path": "/", "source": "command", "versioned": false}, {"path": "/docs/", "source": "vitepress", "versioned": true}]'
      site_title: "Golden Render"
      link_rot_label: "docs-link-rot"
      setup: bun,node,deno,uv
      install_command: "bun install --frozen-lockfile"
      build_command: "bun run build"
      dist_dir: "dist"
      custom_domain: ${{ vars.CUSTOM_DOMAIN }}
    permissions:
      contents: read
      pages: write
      id-token: write
      issues: write
