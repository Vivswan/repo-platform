{
  "name": "validate-template-report",
  "private": true,
  "description": "Dependency manifest for the validate-template-report action (dev-only: zod satisfies the shared runtime.ts helpers' type-only import; the scripts carry no runtime dependencies).",
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2",
    "zod": "^4.4.3"
  }
}
