{
  "name": "validate-managed-files",
  "private": true,
  "description": "Dependency manifest for the validate-managed-files action: yaml is the hygiene validator's runtime dependency (it parses every YAML file it scans); the step scripts under src/ carry none, and check.ts runs from the platform checkout with that checkout's dependencies.",
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "^7.0.2"
  }
}
