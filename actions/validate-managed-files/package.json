{
  "name": "validate-managed-files",
  "private": true,
  "description": "Dependency manifest for the validate-managed-files action: yaml is the validator's runtime dependency (it parses the registration, the module data file, and every YAML file it scans); the report scripts under src/ carry none.",
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
