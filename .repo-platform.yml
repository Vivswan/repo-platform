# The operator's own registration: this repository is a settings target like any other, and
# `bun run settings` renders its settings.yml from the modules here.
# The sync never targets this repository (its files are the sources).
modules:
  - bun
  - pr-title
  - site
project:
  name: repo-platform
  slug: repo-platform
  description: Standards files, CI, and settings for Vivswan's repositories, pushed from one place
  copyright_holder: Vivswan Shah (https://github.com/Vivswan)
