# Reusable template sync: pulls the latest repo-platform release into the
# calling repository via `copier update` and opens a PR to itself.
#
# Token model: uses REPO_PLATFORM_TOKEN when the caller passes it (PR is
# authored by the user and triggers CI normally); falls back to the run's
# GITHUB_TOKEN otherwise (PR is created but CI won't auto-trigger - the PR
# body carries a close/reopen note).

name: Reusable Template Sync

on:
  workflow_call:
    inputs:
      version:
        description: Template release tag to sync to (empty = latest release)
        required: false
        type: string
        default: ""
    secrets:
      REPO_PLATFORM_TOKEN:
        required: false

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: template-sync-${{ github.repository }}
  cancel-in-progress: false

env:
  TEMPLATE_REPO: Vivswan/repo-platform

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Resolve token mode
        id: token
        env:
          PAT: ${{ secrets.REPO_PLATFORM_TOKEN }}
        run: |
          if [ -n "$PAT" ]; then
            echo "mode=pat" >> "$GITHUB_OUTPUT"
          else
            echo "mode=github-token" >> "$GITHUB_OUTPUT"
          fi

      - name: Check out repository
        uses: actions/checkout@v7
        with:
          # Always base the update on the default branch, not the triggering
          # ref - a dispatch from a feature branch must not leak its commits
          # into the automation PR.
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          token: ${{ secrets.REPO_PLATFORM_TOKEN || github.token }}

      - name: Require registration files
        run: |
          test -f .repo-platform.yml || { echo "::error::.repo-platform.yml missing - repo is not registered for template sync"; exit 1; }
          test -f .copier-answers.yml || { echo "::error::.copier-answers.yml missing - repo was not generated by copier"; exit 1; }

      - name: Determine current and target versions
        id: versions
        env:
          GH_TOKEN: ${{ github.token }}
          REQUESTED: ${{ inputs.version }}
        run: |
          current="$(awk '$1 == "_commit:" { print $2 }' .copier-answers.yml)"
          if [ -n "$REQUESTED" ]; then
            target="$REQUESTED"
          else
            target="$(gh api "repos/${TEMPLATE_REPO}/releases/latest" --jq .tag_name)"
          fi
          echo "current=${current}" >> "$GITHUB_OUTPUT"
          echo "target=${target}" >> "$GITHUB_OUTPUT"
          if [ "$current" = "$target" ]; then
            echo "Already on ${current}; nothing to do."
            echo "up_to_date=true" >> "$GITHUB_OUTPUT"
          else
            echo "Updating ${current} -> ${target}"
            echo "up_to_date=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install copier
        if: steps.versions.outputs.up_to_date == 'false'
        run: pipx install copier

      - name: Create update branch
        if: steps.versions.outputs.up_to_date == 'false'
        env:
          TARGET: ${{ steps.versions.outputs.target }}
        run: git checkout -B "automation/repo-platform-${TARGET}"

      - name: Apply copier update
        if: steps.versions.outputs.up_to_date == 'false'
        env:
          TARGET: ${{ steps.versions.outputs.target }}
        run: |
          copier update --vcs-ref "$TARGET" --defaults --trust

      - name: Check out repo-platform for validation
        if: steps.versions.outputs.up_to_date == 'false'
        uses: actions/checkout@v7
        with:
          repository: Vivswan/repo-platform
          ref: ${{ steps.versions.outputs.target }}
          path: .repo-platform-src

      - name: Validate generated files
        if: steps.versions.outputs.up_to_date == 'false'
        run: |
          pip install --quiet pyyaml
          python3 .repo-platform-src/actions/validate-template/validate_generated_files.py .
          rm -rf .repo-platform-src

      - name: Detect changes
        if: steps.versions.outputs.up_to_date == 'false'
        id: changes
        run: |
          rm -rf .repo-platform-src
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes produced by the update."
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: steps.versions.outputs.up_to_date == 'false' && steps.changes.outputs.changed == 'true'
        env:
          TARGET: ${{ steps.versions.outputs.target }}
        run: |
          git config user.name "repo-platform-sync"
          git config user.email "repo-platform-sync@users.noreply.github.com"
          git add --all
          git commit -m "chore: update repo-platform template to ${TARGET}"
          git push --force origin "automation/repo-platform-${TARGET}"

      - name: Create pull request
        if: steps.versions.outputs.up_to_date == 'false' && steps.changes.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.REPO_PLATFORM_TOKEN || github.token }}
          TARGET: ${{ steps.versions.outputs.target }}
          CURRENT: ${{ steps.versions.outputs.current }}
          TOKEN_MODE: ${{ steps.token.outputs.mode }}
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          branch="automation/repo-platform-${TARGET}"
          existing="$(gh pr list --head "$branch" --json number --jq '.[0].number // empty')"
          if [ -n "$existing" ]; then
            echo "PR #${existing} already exists for ${branch}; branch was updated."
            exit 0
          fi

          body="Automated template update from [\`${TEMPLATE_REPO}\`](https://github.com/${TEMPLATE_REPO}/releases/tag/${TARGET}).

          - Previous: \`${CURRENT}\`
          - New: \`${TARGET}\`

          Review any merge conflicts and confirm repository-local sections were preserved before merging."

          if [ "$TOKEN_MODE" = "github-token" ]; then
            body="${body}

          > [!NOTE]
          > Created with the default GITHUB_TOKEN, so CI did not auto-trigger.
          > Close and reopen this PR (or push to the branch) to run checks."
          fi

          gh pr create \
            --base "$BASE_BRANCH" \
            --head "$branch" \
            --title "chore: update repo-platform template to ${TARGET}" \
            --body "$body"
