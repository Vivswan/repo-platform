# Repo-owned CI checks, called by the managed ci.yml inside the all-green
# gate: every job here must pass before the all-green verdict goes green.
# Generated once
# by {{ github_username }}/repo-platform and never overwritten by template
# sync, so edit freely: multiple jobs, matrices, and further local reusable
# workflows all work.
#
# Two limits from running inside the `checks` caller: jobs here get no
# secrets and are capped at the caller's permissions (contents: read), and
# the all-green verdict treats a skipped job as standing down, so a job
# skipped by its own `if:` does not fail the gate (name a job `info-*` to
# keep even its failures out of the gate). A check that needs secrets, more
# permissions, or must never be skippable, belongs in its own workflow wired
# into the ruleset via .github/settings.yml.

name: Checks

on:
  workflow_call:

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
{% if 'bun' in modules %}
      # Example bun checks; replace the placeholder below with real ones.
      # - uses: oven-sh/setup-bun@v2
      #   with:
      #     bun-version-file: .bun-version
      # - run: bun install --frozen-lockfile
      # - run: bun run lint
      # - run: bun test
{% endif %}{% if 'node' in modules %}
      # Example node checks; replace the placeholder below with real ones.
      # - uses: actions/setup-node@v7
      #   with:
      #     node-version-file: .node-version
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test
{% endif %}{% if 'deno' in modules %}
      # Example deno checks; replace the placeholder below with real ones.
      # - uses: denoland/setup-deno@v2
      #   with:
      #     deno-version-file: .dvmrc
      # - run: deno ci
      # - run: deno lint
      # - run: deno fmt --check
      # - run: deno test
{% endif %}{% if 'uv' in modules %}
      # Example uv checks; replace the placeholder below with real ones.
      # - uses: astral-sh/setup-uv@v10.0.1
      # - run: uv sync
      # - run: uv run pytest
{% endif %}{% if ('bun' in modules) or ('node' in modules) or ('deno' in modules) or ('uv' in modules) %}
{% endif %}      - name: No repository checks yet
        run: echo "add this repository's test and lint jobs to checks.yml"
