---
# Layer 2 of the settings merge, PRIVATE repositories only (docs/settings.md).
# The apply injects this label into its managed set itself (github-settings-as-code pins its private report issue with it); declaring it here keeps the roster the layers spell out equal to the one the apply reconciles.

labels:
  - name: settings-as-code-report
    color: "0e2a47"
    description: managed by settings-as-code private reporting - do not remove
