# yaml-language-server: $schema=../module.schema.json
# Module manifest (schema and loader: scripts/module_manifests.ts).
description: Conventional Commit PR title check, its own required workflow
ownership:
  - { path: .github/workflows/pr-title.yml, class: managed }
settings_layers:
  - settings.yml
