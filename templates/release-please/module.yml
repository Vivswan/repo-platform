# yaml-language-server: $schema=../module.schema.json
# Module manifest (schema and loader: scripts/module_manifests.ts).
description: gate-downstream release job in ci.yml + autorelease labels
ownership:
  - { path: .github/workflows/release.yml, class: managed }
  - { path: .github/workflows/update-release-pr.yml, class: starter }
  - { path: .github/workflows/update-release.yml, class: starter }
  - { path: .release-please-manifest.json, class: starter }
  - { path: release-please-config.json, class: starter }
settings_layers:
  - settings.yml
