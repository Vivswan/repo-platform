# Composer manifest (optional per module folder; every key optional).
# This module owns no files: selecting it gates OFF the base fleet LICENSE
# (see the conditional filename in templates/base/), leaving the repo's own
# license file repo-owned. The folder exists because every module choice in
# copier.yml must have a templates/ folder (check_ssot's module-list rule).
