# yaml-language-server: $schema=../module.schema.json
# Module manifest (schema and loader: scripts/module_manifests.ts).
# This module owns no files: selecting it gates OFF the base fleet LICENSE
# (see the conditional filename in templates/base/), leaving the repo's own
# license file repo-owned. The folder exists because every module in
# MODULE_ORDER must carry a manifest.
description: repo carries its own license in LICENSE.md; the fleet license is not rendered
