# This file is managed by {{ github_username }}/repo-platform.
# Its presence marks this repository as intentionally participating in
# template updates; the sync workflow refuses to run without it.

template:
  repository: {{ github_username }}/repo-platform
  modules: {{ modules | tojson }}

update:
  enabled: true
  auto_merge: false
