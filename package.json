{
  "name": "repo-platform",
  "private": true,
  "description": "Tooling manifest for repo-platform (biome, husky); the template and actions carry their own dependencies.",
  "scripts": {
    "check": "bun run lint && bun run lint:actions && bun run typecheck && bun run typecheck:py && bun run typography && bun run lint:yaml && bun run validate && bun run gitignore:check && bun run compose:check",
    "check:fix": "bun x biome check --write . && uvx ruff check --fix . && uvx ruff format .",
    "lint": "bun x biome ci . && uvx ruff check . && uvx ruff format --check .",
    "lint:actions": "if command -v actionlint >/dev/null; then actionlint; else echo 'actionlint not installed locally; CI runs it (brew install actionlint)'; fi",
    "typecheck": "(cd actions/check-typography && bun x tsc -p .) && (cd actions/validate-commit-names && bun x tsc -p .)",
    "typecheck:py": "uvx --with pyyaml ty check .",
    "typography": "bun actions/check-typography/check-typography.ts .",
    "lint:yaml": "uvx yamllint -s .",
    "validate": "uv run actions/validate-template/validate_generated_files.py --self .",
    "gitignore:check": "python3 scripts/build_gitignore.py --check",
    "compose": "uv run scripts/compose_template.py",
    "compose:check": "uv run .github/scripts/build_branch_tree.py --dest /tmp/repo-platform-compose-check --channel staging && rm -rf /tmp/repo-platform-compose-check",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "husky": "^9.1.7"
  }
}
